(function(){
  'use strict';
  var state={daily:null,week:null,month:null,logger:null,homeDate:null,homeTab:'overview',programTab:'overview',allExercises:false};
  function desktop(){return matchMedia('(min-width:901px)').matches;}
  function data(){try{return DATA||{};}catch(e){return window.DATA||{};}}
  function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
  function num(v){if(v==null||v===''||v===false)return null;var n=Number(v);return Number.isFinite(n)&&n>=0?n:null;}
  function first(){for(var i=0;i<arguments.length;i++){var n=num(arguments[i]);if(n!=null)return n;}return null;}
  function dateNow(){var d=new Date(),m=String(d.getMonth()+1).padStart(2,'0'),day=String(d.getDate()).padStart(2,'0');return d.getFullYear()+'-'+m+'-'+day;}
  function parse(s){var p=String(s||'').split('-').map(Number);return new Date(p[0],p[1]-1,p[2]||1);}
  function fmt(d){return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0');}
  function add(s,n){var d=parse(s);d.setDate(d.getDate()+n);return fmt(d);}
  function monday(s){var d=parse(s),day=d.getDay();d.setDate(d.getDate()+(day===0?-6:1-day));return fmt(d);}
  function monthKey(s){return String(s||dateNow()).slice(0,7);}
  function monthShift(ym,n){var p=ym.split('-').map(Number),d=new Date(p[0],p[1]-1+n,1);return fmt(d).slice(0,7);}
  function displayDate(s,opt){if(!s)return '—';try{return new Intl.DateTimeFormat('tr-TR',opt?Object.assign({timeZone:'UTC'},opt):{day:'numeric',month:'long',year:'numeric',timeZone:'UTC'}).format(new Date(s+'T12:00:00Z'));}catch(e){return s;}}
  function valid(v){if(v==null||v===''||v===false)return false;if(typeof v==='number')return Number.isFinite(v)&&v>=0;var s=String(v).trim().toLowerCase();return s!==''&&s!=='undefined'&&s!=='null'&&s!=='nan'&&s!=='-1';}
  function friendly(v,type){if(!valid(v))return null;var s=String(v).trim(),low=s.toLowerCase();if(type==='nightly'){var n=num(v),night={1:'Çok zayıf',2:'Zayıf',3:'Orta',4:'İyi',5:'Çok iyi',6:'Çok iyi'};if(n!=null&&night[Math.round(n)])return night[Math.round(n)];}var map={'very good':'Çok iyi','good':'İyi','okay':'Orta','ok':'Orta','compromised':'Zayıf','poor':'Zayıf','very poor':'Çok zayıf','none':'Yok','bad':'Kötü','productive':'Üretken','maintaining':'Korunuyor','detraining':'Düşüyor','overreaching':'Aşırı yük','undertraining':'Düşük yük','balanced':'Dengeli','normal':'Normal','controlled':'Kontrollü'};return map[low]||label(s);}
  function value(v,unit){return !valid(v)?'—':esc(v)+(unit?' <em>'+esc(unit)+'</em>':'');}
  function metric(labelText,v,unit){if(!valid(v))return '';return '<div class="desktopMetric"><small>'+esc(labelText)+'</small><b>'+value(v,unit)+'</b></div>';}
  function duration(v){if(v==null||v==='')return null;if(typeof v==='string'&&v.indexOf(':')>-1)return v;var mins=num(v);if(mins==null)return String(v);if(mins>10000)mins/=60;var h=Math.floor(mins/60),m=Math.round(mins%60);return h?h+' sa '+m+' dk':m+' dk';}
  function round(v){var n=num(v);if(n==null)return null;var r=Math.round(n*10)/10;return Number.isInteger(r)?Math.round(r):r;}
  function label(v){var s=String(v||'Polar Workout').replace(/[_-]+/g,' ').toLowerCase();return s.replace(/(^|\s)\S/g,function(c){return c.toUpperCase();});}
  function daily(store,date){return store&&store.daily&&store.daily[date]||null;}
  function list(v){return v==null?[]:(Array.isArray(v)?v:[v]);}
  function workoutsOn(date){return list(daily(data().polarWorkouts,date)).filter(Boolean);}
  function gymOn(date){return (data().workouts||[]).filter(function(w){return w&&w.date===date;});}
  function sessionOn(date){return window.SimurgWorkoutSource&&window.SimurgWorkoutSource.day?window.SimurgWorkoutSource.day(date):{date:date,polar:workoutsOn(date),gym:gymOn(date),primaryPolar:workoutsOn(date)[0]||null,extraPolar:workoutsOn(date).slice(1),source:workoutsOn(date).length&&gymOn(date).length?'Polar + Gym':workoutsOn(date).length?'Polar':gymOn(date).length?'Gym':null};}
  function sessionCount(date){var s=sessionOn(date);return s.polar.length||(s.gym.length?1:0)||(s.appleLegacy?1:0);}
  function activityOn(date){return daily(data().polarActivity,date);}
  function sleepOn(date){return daily(data().polarSleep,date);}
  function nightlyOn(date){return daily(data().polarNightlyRecharge,date);}
  function loadOn(date){return daily(data().polarCardioLoad,date);}
  function noteOn(date){return (data().dailyNotes||[]).filter(function(x){return x&&x.date===date;}).slice(-1)[0]||null;}
  function meaningful(value,key){if(value==null||value===false)return false;if(Array.isArray(value))return value.some(function(v){return meaningful(v,key);});if(typeof value==='number')return Number.isFinite(value)&&value>0;if(typeof value==='string'){var s=value.trim().toLowerCase();if(!s||s==='-1'||s==='0'||s==='null'||s==='undefined'||s==='nan'||s==='missing'||s==='waiting'||s==='unavailable'||s==='no data'||s==='veri yok')return false;return !/^(date|source|status|type)$/.test(String(key||''));}if(typeof value==='object')return Object.keys(value).some(function(k){if(/^(date|source|status|lastSyncAt|lastError|connected|receivedAt)$/i.test(k))return false;return meaningful(value[k],k);});return false;}
  function homeDayHasData(date){var d=data(),gym=gymOn(date),stores=['polarWorkouts','polarActivity','polarSleep','polarNightlyRecharge','polarCardioLoad'];if(gym.some(function(x){return meaningful(x);}))return true;if(stores.some(function(k){return meaningful(daily(d[k],date));}))return true;return meaningful(noteOn(date));}
  function homeDataDates(){var d=data(),dates=[];(d.workouts||[]).forEach(function(x){if(x&&x.date&&meaningful(x))dates.push(x.date);});['polarWorkouts','polarActivity','polarSleep','polarNightlyRecharge','polarCardioLoad'].forEach(function(k){var store=d[k]&&d[k].daily||{};Object.keys(store).forEach(function(date){if(meaningful(store[date]))dates.push(date);});});(d.dailyNotes||[]).forEach(function(x){if(x&&x.date&&meaningful(x))dates.push(x.date);});return [...new Set(dates.filter(function(x){return /^\d{4}-\d{2}-\d{2}$/.test(x);} ))].sort();}
  function latestHomeDate(){var dates=homeDataDates();return dates.length?dates[dates.length-1]:dateNow();}
  function stats(rows){var ex={};rows.forEach(function(r){if(r.exercise)ex[r.exercise]=1;});return {sets:rows.length,reps:rows.reduce(function(a,r){return a+(num(r.reps)||0);},0),volume:rows.reduce(function(a,r){return a+(num(r.reps)||0)*(num(r.weight)||0);},0),exercises:Object.keys(ex).length};}
  function allDates(){var d=data(),keys=[];[(d.polarActivity||{}).daily,(d.polarSleep||{}).daily,(d.polarNightlyRecharge||{}).daily,(d.polarCardioLoad||{}).daily,(d.polarWorkouts||{}).daily].forEach(function(x){keys=keys.concat(Object.keys(x||{}));});keys=keys.concat((d.workouts||[]).map(function(x){return x.date;}));return keys.filter(Boolean).sort();}
  function recentValues(store,reader,count,throughDate){var dailyStore=store&&store.daily||{};return Object.keys(dailyStore).filter(function(k){return !throughDate||k<=throughDate;}).sort().slice(-(count||7)).map(function(k){return reader(dailyStore[k]);}).filter(function(v){return num(v)!=null;});}
  function segmented(kind,tabs,active){return '<div class="desktopSegmented" role="tablist">'+tabs.map(function(tab){return '<button type="button" role="tab" aria-selected="'+(tab.id===active?'true':'false')+'" class="'+(tab.id===active?'active':'')+'" onclick="'+(kind==='home'?'desktopSetHomeTab':'desktopSetProgramTab')+'(\''+tab.id+'\')">'+esc(tab.label)+'</button>';}).join('')+'</div>';}
  function latestDate(){var dates=allDates();return dates.length?dates[dates.length-1]:dateNow();}
  function clean(obj){if(obj==null)return obj;if(Array.isArray(obj))return obj.map(clean).filter(function(v){return v!==undefined;});if(typeof obj==='object'){var out={};Object.keys(obj).forEach(function(k){var v=clean(obj[k]);if(v!==undefined&&v!==null&&!(Array.isArray(v)&&!v.length)&&!(typeof v==='object'&&!Array.isArray(v)&&!Object.keys(v).length))out[k]=v;});return out;}if(typeof obj==='number'&&!Number.isFinite(obj))return undefined;return obj;}
  function dayPayload(date){var gym=gymOn(date),polar=workoutsOn(date),activity=activityOn(date),sleep=sleepOn(date),nightly=nightlyOn(date),load=loadOn(date),note=noteOn(date);return clean({date:date,activity:activity,sleep:sleep,nightlyRecharge:nightly,cardioLoad:load,polarWorkouts:polar,gymWorkout:gym.length?{program:typeof getProgramType==='function'?getProgramType(typeof dayName==='function'?dayName(date):''):null,summary:stats(gym),sets:gym}:null,dailyNote:note});}
  function downloadJson(name,payload){var blob=new Blob([JSON.stringify(clean(payload),null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;document.body.appendChild(a);a.click();setTimeout(function(){URL.revokeObjectURL(a.href);a.remove();},0);}
  function stageRows(sleep){if(!sleep)return '';var rows=[['Deep',sleep.deepSleep,'#555ee7'],['REM',sleep.remSleep,'#20cbd8'],['Light',sleep.lightSleep,'#4aa9e8'],['Awake',sleep.awakeTime,'#c77c68']],available=rows.filter(function(r){return num(r[1])!=null;}),total=available.reduce(function(a,r){return a+num(r[1]);},0);if(!total)return '';return '<div class="desktopStageBar">'+available.map(function(r){return '<i style="width:'+(num(r[1])/total*100).toFixed(2)+'%;background:'+r[2]+'"></i>';}).join('')+'</div><div class="desktopMuted">'+available.map(function(r){return esc(r[0])+' '+esc(duration(num(r[1])/60));}).join(' · ')+'</div>';}
  function ring(v,labelText,color,display){var n=num(v),pct=n==null?0:Math.max(0,Math.min(100,n));return '<div class="desktopRing" style="--ring-value:'+pct+';--ring-color:'+(color||'#10b9ff')+'"><div><b>'+esc(display!=null?display:(n==null?'—':round(n)))+'</b><small>'+esc(labelText)+'</small></div></div>';}
  function progress(v,color){var n=num(v),pct=n==null?0:Math.max(0,Math.min(100,n));return '<div class="desktopProgress '+(color||'')+'"><i style="width:'+pct+'%"></i></div>';}
  function spark(values){var clean=values.map(num).filter(function(v){return v!=null;}),max=Math.max.apply(null,clean.concat([1]));return clean.length?'<div class="desktopSpark">'+clean.map(function(v){return '<i style="height:'+Math.max(8,v/max*100)+'%"></i>';}).join('')+'</div>':'';}
  function badge(v,type){var text=friendly(v,type);if(!text)return '';var low=text.toLowerCase(),tone=/iyi|dengeli|normal|kontrollü|üretken/.test(low)?' good':/zayıf|orta|korunuyor|düşük/.test(low)?' warn':/kötü|aşırı|risk/.test(low)?' danger':'';return '<span class="desktopBadge'+tone+'">'+esc(text)+'</span>';}
  function stateBadge(v,type){var text=friendly(v,type);if(!text)return '<span class="desktopStateBadge">—</span>';var low=text.toLowerCase(),tone=/iyi|yok|normal|kontrollü/.test(low)?' good':/orta/.test(low)?' warn':/kötü|pain|ağrı/.test(low)?' danger':'';return '<span class="desktopStateBadge'+tone+'">'+esc(text)+'</span>';}
  function zoneRows(zones){if(!zones)return '';var colors=['#8aa0b3','#168ed5','#63b72f','#e98a22','#e33a46'],rows=[];for(var i=1;i<=5;i++){var raw=zones['zone'+i]||zones['Zone '+i],seconds=0;if(typeof raw==='number')seconds=raw;else if(raw){var p=String(raw).split(':').map(Number);seconds=p.length===3?p[0]*3600+p[1]*60+p[2]:p.length===2?p[0]*60+p[1]:0;}rows.push({i:i,seconds:seconds});}var total=rows.reduce(function(a,x){return a+x.seconds;},0);if(!total)return '';return '<div class="desktopZoneRows">'+rows.reverse().map(function(x){var pct=Math.round(x.seconds/total*100);return '<div class="desktopZoneRow"><b>Zone '+x.i+'</b><span><i style="width:'+pct+'%;background:'+colors[x.i-1]+'"></i></span><em>'+pct+'%</em></div>';}).join('')+'</div>';}
  function bars(items){var max=Math.max.apply(null,items.map(function(x){return x.value;}).concat([1]));return '<div class="desktopBarRows">'+items.map(function(x){return '<div class="desktopBarRow"><b>'+esc(x.label)+'</b><span><i style="width:'+(x.value/max*100).toFixed(1)+'%"></i></span><em>'+esc(x.display!=null?x.display:x.value)+'</em></div>';}).join('')+'</div>';}
  function lineSpark(values,color){var clean=values.map(num).filter(function(v){return v!=null;});if(clean.length<2)return '';var min=Math.min.apply(null,clean),max=Math.max.apply(null,clean),range=max-min||1,points=clean.map(function(v,i){return (i/(clean.length-1)*100).toFixed(2)+','+(38-(v-min)/range*31).toFixed(2);}).join(' '),last=clean[clean.length-1],tone=color||'#18c8ff';return '<div class="desktopLineChart"><svg viewBox="0 0 100 42" preserveAspectRatio="none" aria-hidden="true"><polygon points="0,42 '+points+' 100,42" fill="'+tone+'" opacity=".12"/><polyline points="'+points+'" fill="none" stroke="'+tone+'" stroke-width="1.8" vector-effect="non-scaling-stroke"/></svg><b>'+esc(round(last))+'</b></div>';}
  function signalRow(icon,labelText,v,max,color,status){if(!valid(v))return '';var n=num(v),width=n==null?55:Math.max(5,Math.min(100,n/(max||100)*100));return '<div class="desktopSignal"><i>'+esc(icon)+'</i><div><span>'+esc(labelText)+'</span><em><u style="width:'+width+'%;background:'+(color||'#26d98b')+'"></u></em></div><b>'+esc(v)+'</b>'+badge(status)+'</div>';}
  function homeRingCard(title,v,status,color,details,action){if(!valid(v)&&!details)return card(title,'<div class="desktopEmpty">Henüz yeterli veri yok.</div>','homeRingCard',action);return card(title,'<div class="homeRingBody">'+ring(v,title,color)+'<div>'+badge(status)+'<p>'+esc(details||'Mevcut gerçek veri')+'</p></div></div>','homeRingCard',action);}
  function donut(items){var validItems=items.filter(function(x){return num(x.value)>0;}),total=validItems.reduce(function(a,x){return a+num(x.value);},0);if(!total)return '';var cursor=0,stops=[];validItems.forEach(function(x){var start=cursor,end=cursor+num(x.value)/total*100;stops.push((x.color||'#2f80ff')+' '+start.toFixed(1)+'% '+end.toFixed(1)+'%');cursor=end;});return '<div class="desktopDonutWrap"><div class="desktopDonut" style="background:conic-gradient('+stops.join(',')+')"><span><b>'+validItems.length+'</b><small>Grup</small></span></div><div class="desktopDonutLegend">'+validItems.map(function(x){return '<div><i style="background:'+x.color+'"></i><span>'+esc(x.label)+'</span><b>'+Math.round(num(x.value)/total*100)+'%</b></div>';}).join('')+'</div></div>';}
  function card(title,body,cls,action){var tag=action?'button':'article';return '<'+tag+' class="desktopCard '+(cls||'')+(action?' clickable':'')+'"'+(action?' onclick="'+action+'"':'')+'><div class="desktopCardHead"><h2>'+esc(title)+'</h2></div>'+body+'</'+tag+'>';}
  function nav(kind,labelText){return '<div class="desktopNav"><button onclick="desktopMove(\''+kind+'\',-1)">← Önceki</button><strong>'+esc(labelText)+'</strong><button onclick="desktopMove(\''+kind+'\',1)">Sonraki →</button><button onclick="desktopLatest(\''+kind+'\')">Bugün / Latest</button></div>';}
  function homeDateNav(date){return '<div class="desktopHomeDateNav" aria-label="Home tarih navigasyonu"><button type="button" onclick="desktopHomeMove(-1)">← Önceki</button><strong>'+esc(displayDate(date,{day:'numeric',month:'short',year:'numeric'}))+'</strong><button type="button" onclick="desktopHomeMove(1)">Sonraki →</button><button class="secondary" type="button" onclick="desktopHomeToday()">Bugün</button><button class="latest" type="button" onclick="desktopHomeLatest()">Latest</button></div>';}
  function ensureNav(){var buttons=[].slice.call(document.querySelectorAll('aside .nav button'));buttons.forEach(function(b){if(!b.dataset.desktopOriginalHtml){b.dataset.desktopOriginalHtml=b.innerHTML;b.dataset.desktopOriginalText=b.textContent.trim();}if(!desktop()){if(b.dataset.desktopLabel){b.innerHTML=b.dataset.desktopOriginalHtml;b.removeAttribute('aria-label');}delete b.dataset.desktopHidden;return;}var t=b.dataset.desktopOriginalText||b.textContent,order=20,name='',icon='';if(t.indexOf('Ana Sayfa')>-1){order=1;name='Home';icon='🏠';}else if(t.indexOf('Workout Logger')>-1){order=2;name='Workout Logger';icon='🏋️';}else if(t.indexOf('Daily Summary')>-1){order=3;name='Daily Summary';icon='📝';}else if(t.indexOf('Weekly Summary')>-1){order=4;name='Weekly Summary';icon='📅';}else if(t.indexOf('Monthly Review')>-1){order=5;name='Monthly Review';icon='🗓️';}else if(t.indexOf('Program Intelligence')>-1){order=6;name='Program Intelligence';icon='🧭';}else if(t.indexOf('Coaching')>-1){order=7;name='Coaching';icon='🧠';}else if(t.indexOf('Data Center')>-1){order=8;name='Data Center';icon='💾';}else if(/Toparlanma|Gym Mode|Progress|Polar Workout/.test(t)){b.dataset.desktopHidden='1';}if(name){var accessible=icon+' '+name;b.dataset.desktopLabel=accessible;b.setAttribute('aria-label',accessible);b.style.setProperty('--desktop-order',order);if(!b.querySelector('.desktopNavLabel'))b.innerHTML='<span class="desktopNavIcon">'+icon+'</span><span class="desktopNavLabel">'+esc(name)+'</span>';}});}
  function home(){
    var root=document.getElementById('home');if(!root)return;var old=root.querySelector('#desktopHome');if(!desktop()){if(old)old.remove();return;}
    var d=data(),date=state.homeDate||dateNow(),act=activityOn(date),sleep=sleepOn(date),night=nightlyOn(date),load=loadOn(date),session=sessionOn(date),polar=session.polar,latest=act||null,gym=session.gym,dayNote=noteOn(date),hasDayData=homeDayHasData(date);
    var steps=first(act&&act.steps,act&&act.stepCount),cal=first(act&&act.activeCalories,act&&act.activeCal),activeDuration=duration(first(act&&act.activeTime,act&&act.duration)),completion=(polar.length||gym.length)?100:(steps!=null?Math.min(100,Math.round(steps/100)):null);
    var activityMetrics=metric('Adım',steps)+metric('Aktif kalori',cal,'kcal')+metric('Aktivite süresi',activeDuration)+metric('Workout',sessionCount(date)||null);
    var activityBody='<div class="desktopVisualHero">'+ring(completion,'Günlük durum','#10b9ff',completion==null?'—':completion+'%')+'<div class="desktopHeroCopy"><b class="desktopHeroValue">'+esc(activeDuration||cal&&round(cal)+' kcal'||steps&&round(steps)+' adım'||'Veri bekleniyor')+'</b>'+badge((polar.length||gym.length)?'Workout kayıtlı':'Normal')+(completion!=null?progress(completion):'')+'</div></div>'+(activityMetrics?'<div class="desktopMetrics">'+activityMetrics+'</div>':'')+(latest?'<div class="desktopListItem"><b>⌁ '+esc(label(latest.activityType||latest.workoutType||'Son aktivite'))+'</b>'+esc(displayDate(latest.date||date))+'</div>':'<div class="desktopEmpty">Bugün için aktivite verisi yok.</div>');
    var nightlyValue=first(night&&night.nightlyRechargeStatus,night&&night.status),hrv=first(night&&night.heartRateVariabilityAvg,night&&night.hrv),rhr=first(night&&night.heartRateAvg,night&&night.restingHR),ans=first(night&&night.ansCharge,night&&night.ansChargeStatus),recoveryRing=first(night&&night.recoveryScore,night&&night.nightlyRechargeScore);
    var recoveryMetrics=metric('HRV',hrv,'ms')+metric('Dinlenik nabız',rhr,'bpm');
    var recoveryBody=(night||recoveryMetrics)?'<div class="desktopVisualHero">'+ring(recoveryRing,'Recovery','#80c72e',friendly(nightlyValue,'nightly')||'Sinyal')+'<div class="desktopHeroCopy">'+badge(nightlyValue,'nightly')+badge(ans,'nightly')+'<p>Mevcut Polar recovery sinyalleri; yeni readiness skoru üretilmez.</p></div></div>'+(recoveryMetrics?'<div class="desktopMetrics">'+recoveryMetrics+'</div>':''):'<div class="desktopEmpty">Henüz yeterli recovery verisi yok.</div>';
    var sleepMinutes=first(sleep&&sleep.durationMinutes,sleep&&sleep.durationSeconds!=null?sleep.durationSeconds/60:null),sleepScore=first(sleep&&sleep.sleepScore),sleepMetrics=metric('Başlangıç',sleep&&sleep.sleepStartTime)+metric('Bitiş',sleep&&sleep.sleepEndTime);
    var sleepBody=sleep?'<div class="desktopVisualHero">'+ring(sleepScore,'Sleep','#8b5cf6')+'<div class="desktopHeroCopy"><b class="desktopHeroValue">'+esc(duration(sleepMinutes)||'—')+'</b>'+progress(sleepScore,'purple')+'<p>Toplam uyku ve Polar Sleep Stages dağılımı.</p></div></div>'+(sleepMetrics?'<div class="desktopMetrics">'+sleepMetrics+'</div>':'')+stageRows(sleep):'<div class="desktopEmpty">Henüz yeterli uyku verisi yok.</div>';
    var cardio=first(load&&load.cardioLoad,load&&load.load),strain=first(load&&load.strain),tolerance=first(load&&load.tolerance),loadStatus=friendly(load&&load.status),loadMetrics=metric('Strain',round(strain))+metric('Tolerance',round(tolerance))+metric('Son workout load',polar.length?round(first(polar[polar.length-1].trainingLoad,polar[polar.length-1].cardioLoad)):null);
    var loadBody=(cardio!=null||strain!=null||tolerance!=null)?'<div class="desktopVisualHero">'+ring(first(cardio,strain),'Cardio Load','#10b9ff')+'<div class="desktopHeroCopy">'+badge(loadStatus||'Load')+(strain!=null?progress(strain):'')+'<p>Strain ve tolerance mevcut Polar değerleriyle gösterilir.</p></div></div>'+(loadMetrics?'<div class="desktopMetrics">'+loadMetrics+'</div>':''):'<div class="desktopEmpty">Henüz yeterli load verisi yok.</div>';
    var tabs=segmented('home',[{id:'overview',label:'Overview'},{id:'recovery',label:'Recovery'},{id:'sleep',label:'Sleep'},{id:'load',label:'Load'}],state.homeTab),content='';
    if(state.homeTab==='overview'){
      var todayActivity=(act||latest||gym.length||polar.length)?card('Daily Activity','<div class="desktopMetrics">'+metric('Adım',steps)+metric('Aktif kalori',cal,'kcal')+metric('Aktivite süresi',activeDuration)+metric('Gym workout',gym.length?'Var':null)+metric('Polar workout',polar.length?'Var':null)+'</div>'+(latest?'<div class="desktopListItem"><b>⌁ '+esc(label(latest.activityType||latest.workoutType||'Son aktivite'))+'</b>'+esc(displayDate(latest.date||date))+'</div>':''),'span6 compactPanel'):'';
      var gs=stats(gym),lastPolar=session.primaryPolar||polar.slice(-1)[0],todayTraining=(gym.length||polar.length||session.appleLegacy)?card('Daily Training','<div class="desktopMetrics">'+metric('Kaynak',session.source)+metric('Workout',lastPolar?label(lastPolar.workoutType||lastPolar.activityType):(session.appleLegacy?label(session.appleLegacy.workoutType||session.appleLegacy.activityType):(typeof getProgramType==='function'&&typeof dayName==='function'?getProgramType(dayName(date)):null)))+metric('Egzersiz',gs.exercises||null)+metric('Set',gs.sets||null)+metric('Tekrar',gs.reps||null)+metric('Hacim',gs.volume?round(gs.volume):null,'kg')+metric('Ort. nabız',round(lastPolar&&lastPolar.avgHR),'bpm')+metric('Polar load',round(lastPolar&&first(lastPolar.trainingLoad,lastPolar.cardioLoad)))+'</div>','span6 compactPanel'):'',summaryPanel=dayNote?card('Daily Summary','<div class="desktopListItem">'+esc(dayNote.note||dayNote.notes||dayNote.text||'Günlük summary kaydı mevcut.')+'</div>','span12 compactPanel'):'';
      var recoveryStatus=friendly(nightlyValue,'nightly'),sleepStatus=sleepScore==null?null:(sleepScore>=80?'İyi':sleepScore>=60?'Orta':'Zayıf'),loadStatusText=loadStatus||((cardio!=null&&cardio>=70)?'Yüksek':cardio!=null?'Orta':null),strainStatus=strain==null?null:(strain>=8?'Yüksek':strain>=4?'Orta':'Düşük');
      content='<div class="desktopGrid desktopHomeOverview">'+homeRingCard('Recovery',recoveryRing,recoveryStatus,'#26d98b',[hrv!=null?'HRV '+round(hrv)+' ms':'',rhr!=null?'RHR '+round(rhr)+' bpm':''].filter(Boolean).join(' · '),"desktopSetHomeTab('recovery')")+homeRingCard('Sleep',sleepScore,sleepStatus,'#8b63ff',[duration(sleepMinutes),sleep&&sleep.remSleep!=null?'REM '+duration(num(sleep.remSleep)/60):''].filter(Boolean).join(' · '),"desktopSetHomeTab('sleep')")+homeRingCard('Load',cardio,loadStatusText,'#2f80ff',[strain!=null?'Strain '+round(strain):'',tolerance!=null?'Tolerance '+round(tolerance):''].filter(Boolean).join(' · '),"desktopSetHomeTab('load')")+homeRingCard('Strain',strain,strainStatus,'#ff8b24',[tolerance!=null?'Tolerance '+round(tolerance):'',loadStatusText||''].filter(Boolean).join(' · '),"desktopSetHomeTab('load')")+todayActivity+todayTraining+summaryPanel+'</div>';
    }else if(state.homeTab==='recovery'){
      var recoveryTrend=recentValues(d.polarNightlyRecharge,function(x){return first(x&&x.heartRateVariabilityAvg,x&&x.hrv,x&&x.nightlyRechargeScore);},7,date),recoveryDetail=metric('Nightly Recharge',friendly(nightlyValue,'nightly'))+metric('ANS Charge',friendly(ans,'nightly'))+metric('Sleep Charge',friendly(first(night&&night.sleepCharge,night&&night.sleepChargeStatus),'nightly'))+metric('HRV',hrv,'ms')+metric('Dinlenik nabız',rhr,'bpm');
      var sleepCharge=first(night&&night.sleepCharge,night&&night.sleepChargeStatus),fatigue=first(night&&night.fatigue,night&&night.fatigueLevel),hasRecovery=[nightlyValue,ans,hrv,rhr,recoveryRing,sleepCharge].some(function(v){return valid(v);}),signals=signalRow('♥','HRV',hrv,100,'#26d98b',hrv!=null?'İyi':null)+signalRow('◉','Dinlenik Nabız',rhr,100,'#26d98b',rhr!=null?'İyi':null)+signalRow('⌁','ANS Charge',ans,100,'#18c8ff',friendly(ans,'nightly'))+signalRow('☾','Sleep Charge',sleepCharge,100,'#8b63ff',friendly(sleepCharge,'nightly'))+signalRow('⚡','Nightly Recharge',nightlyValue,6,'#26d98b',friendly(nightlyValue,'nightly'))+signalRow('!','Yorgunluk',fatigue,10,'#ffac32',fatigue!=null?'Dikkat':null);content=hasRecovery?'<div class="desktopGrid recoveryReferenceGrid"><article class="desktopCard span4 desktopRecoveryScore toneGreen"><h2>Recovery Skoru</h2>'+ring(recoveryRing,'Recovery','#26d98b',friendly(nightlyValue,'nightly')||'Kısmi veri')+'<b class="referenceStatus">'+esc(friendly(nightlyValue,'nightly')||'Kısmi veri')+'</b><p>Vücudun toparlanma sinyallerinin mevcut özeti.</p></article><article class="desktopCard span8"><div class="desktopCardHead"><h2>Recovery Sinyalleri</h2></div><div class="desktopSignals">'+signals+'</div></article><article class="desktopCard span8"><h2>HRV Trendi</h2>'+lineSpark(recoveryTrend,'#26d98b')+'</article><article class="desktopCard span4 desktopInsight"><span class="desktopKicker">Yorum</span><p>Toparlanma verileri mevcut Polar sinyalleriyle gösterilir. Yeni readiness skoru üretilmez.</p></article></div>':'<div class="desktopEmpty desktopTabEmpty">Henüz yeterli recovery verisi yok.</div>';
    }else if(state.homeTab==='sleep'){
      var sleepTrend=recentValues(d.polarSleep,function(x){return first(x&&x.sleepScore,x&&x.durationMinutes);},7,date),sleepDetail=metric('Başlangıç',sleep&&sleep.sleepStartTime)+metric('Bitiş',sleep&&sleep.sleepEndTime)+metric('Efficiency',first(sleep&&sleep.sleepEfficiency,sleep&&sleep.efficiency),'%')+metric('Continuity',first(sleep&&sleep.sleepContinuity,sleep&&sleep.continuity))+metric('Interruptions',first(sleep&&sleep.interruptions,sleep&&sleep.interruptionCount));
      content=sleep?'<div class="desktopGrid sleepReferenceGrid"><article class="desktopCard span4 desktopSleepScore tonePurple"><h2>Uyku Skoru</h2>'+ring(sleepScore,'Sleep','#8b63ff')+'<b class="referenceStatus">'+esc(duration(sleepMinutes)||'—')+'</b>'+badge(sleepScore!=null?(sleepScore>=80?'İyi':sleepScore>=60?'Orta':'Zayıf'):null)+'</article><article class="desktopCard span8"><div class="desktopCardHead"><h2>Uyku Özeti</h2></div><div class="desktopMetrics">'+sleepDetail+'</div><h3>Sleep Stages</h3>'+stageRows(sleep)+'</article><article class="desktopCard span8"><h2>Uyku Trendi</h2>'+lineSpark(sleepTrend,'#8b63ff')+'</article><article class="desktopCard span4 desktopInsight"><span class="desktopKicker">Yorum</span><p>Uyku süresi, skoru ve evreleri mevcut Polar Sleep verisinden gösterilir.</p></article></div>':'<div class="desktopEmpty desktopTabEmpty">Henüz yeterli uyku verisi yok.</div>';
    }else{
      var loadTrend=recentValues(d.polarCardioLoad,function(x){return first(x&&x.cardioLoad,x&&x.strain);},7,date),lastWorkout=polar.slice(-1)[0]||null,loadDetail=metric('Cardio Load',round(cardio))+metric('Strain',round(strain))+metric('Tolerance',round(tolerance))+metric('Latest Workout Load',round(lastWorkout&&first(lastWorkout.trainingLoad,lastWorkout.cardioLoad)));var compare=(strain!=null||tolerance!=null)?'<div class="desktopCompare"><div><b>Strain</b>'+progress(strain,'orange')+'</div><div><b>Tolerance</b>'+progress(tolerance,'green')+'</div></div>':'';
      var distribution=[{label:'Düşük',value:loadTrend.filter(function(v){return v<40;}).length,color:'#26d98b'},{label:'Orta',value:loadTrend.filter(function(v){return v>=40&&v<70;}).length,color:'#2f80ff'},{label:'Yüksek',value:loadTrend.filter(function(v){return v>=70;}).length,color:'#ff8b24'}],loadDonut=donut(distribution);content=(cardio!=null||strain!=null||tolerance!=null||lastWorkout)?'<div class="desktopGrid loadReferenceGrid"><article class="desktopCard span4 desktopLoadPrimary"><h2>Cardio Load</h2>'+ring(cardio,'Load','#2f80ff')+badge(loadStatus||'Load')+'</article><article class="desktopCard span4 toneOrange"><h2>Strain</h2><b class="referenceMetric">'+value(round(strain))+'</b>'+badge(strain==null?null:(strain>=8?'Yüksek':strain>=4?'Orta':'Düşük'))+progress(strain,'orange')+'<p class="desktopMuted">Mevcut yorgunluk ve yük sinyali.</p></article><article class="desktopCard span4"><h2>Load Tolerance</h2><b class="referenceMetric">'+value(round(tolerance))+'</b>'+badge(loadStatus||null)+'<p class="desktopMuted">Antrenman toleransının mevcut Polar değeri.</p></article><article class="desktopCard span12 loadTrendCard"><div><h2>7 Günlük Load Trend</h2>'+lineSpark(loadTrend,'#2f80ff')+'</div>'+(loadDonut?'<div><h2>Load Dağılımı</h2>'+loadDonut+'</div>':'')+'</article>'+(lastWorkout?'<article class="desktopCard span12 compactPanel"><div class="desktopCardHead"><div><span class="desktopKicker">Son Workout</span><h2>'+esc(label(lastWorkout.workoutType||lastWorkout.activityType))+'</h2></div>'+badge(round(first(lastWorkout.trainingLoad,lastWorkout.cardioLoad))+' load')+'</div>'+zoneRows(lastWorkout.zones)+'</article>':'')+'</div>':'<div class="desktopEmpty desktopTabEmpty">Henüz yeterli load verisi yok.</div>';
    }
    if(!hasDayData)content='<div class="desktopEmpty desktopHomeDayEmpty">Bu tarihte kayıtlı veri bulunmuyor.</div>';
    root.querySelector('#desktopHome')?.remove();root.insertAdjacentHTML('beforeend','<div id="desktopHome" class="desktopWorkspace"><header class="desktopHead"><div><span class="desktopKicker">Simurg OS · Home</span><h1>'+esc(displayDate(date,{day:'numeric',month:'long',year:'numeric',weekday:'long'}))+'</h1><p>'+(date===dateNow()?'Bugünün özeti ve genel durumu.':'Seçili günün aktivite, toparlanma, uyku ve yük özeti.')+'</p></div>'+homeDateNav(date)+'</header>'+tabs+'<div class="desktopTabPane">'+content+'</div></div>');
  }
  function workoutTable(rows){if(!rows.length)return '<div class="desktopEmpty">Bu gün için Gym kaydı yok.</div>';var grouped={};rows.forEach(function(r){(grouped[r.exercise||'Egzersiz']=grouped[r.exercise||'Egzersiz']||[]).push(r);});return '<div class="desktopLoggerTableWrap"><table class="desktopLoggerTable"><thead><tr><th>Egzersiz</th><th>Set</th><th>Tekrar</th><th>Ağırlık</th><th>Hacim</th><th>RPE</th><th>Kalite</th><th>Hedef</th></tr></thead><tbody>'+Object.keys(grouped).map(function(ex,i){var sets=grouped[ex],last=sets[sets.length-1],target=(data().autoNextTargets||{})[ex],targetText=sets.map(function(r){return r.autoTarget||r.nextSessionTarget;}).filter(Boolean).slice(-1)[0]||target&&target.text,reps=sets.reduce(function(a,r){return a+(num(r.reps)||0);},0),volume=sets.reduce(function(a,r){return a+(num(r.reps)||0)*(num(r.weight)||0);},0),weights=sets.map(function(r){return num(r.weight);}).filter(function(v){return v!=null;}),rpes=sets.map(function(r){return num(r.rpe);}).filter(function(v){return v!=null;}),notes=[...new Set(sets.map(function(r){return String(r.notes||'').trim();}).filter(Boolean))],body=sets.map(function(r){return r.bodyPart;}).find(Boolean);return '<tr><td><div class="loggerExerciseName"><i>'+['✦','◆','●','✚'][i%4]+'</i><span><b>'+esc(ex)+'</b><small>'+esc(body||'')+(notes.length?' · '+esc(notes.join(' · ')):'')+'</small></span></div></td><td>'+sets.length+'</td><td>'+reps+'</td><td>'+value(weights.length?Math.max.apply(null,weights):null,'kg')+'</td><td>'+value(round(volume),'kg')+'</td><td>'+stateBadge(rpes.length?round(rpes.reduce(function(a,v){return a+v;},0)/rpes.length):null)+'</td><td><div class="loggerQuality">'+stateBadge(last&&last.form)+stateBadge(last&&last.pain)+'</div></td><td><div class="desktopTarget">'+value(targetText)+'</div></td></tr>';}).join('')+'</tbody></table></div>';}
  function polarSummary(date){var rows=workoutsOn(date);if(!rows.length)return '';return '<article class="desktopCard span12"><div class="desktopCardHead"><h2>Linked Polar Workout</h2><span class="desktopBadge">'+rows.length+' seans</span></div>'+rows.map(function(w){return '<div class="desktopSession"><div class="desktopSessionTitle"><h3>⌁ '+esc(label(w.workoutType||w.activityType))+'</h3><span>'+esc(w.startTime||'')+'</span></div><div class="desktopMetrics">'+metric('Süre',duration(w.duration))+metric('Ort. nabız',round(w.avgHR),'bpm')+metric('Maks. nabız',round(w.maxHR),'bpm')+metric('Kalori',round(w.activeCal),'kcal')+metric('Cardio Load',round(first(w.cardioLoad,w.trainingLoad)))+metric('Training Benefit',friendly(w.trainingBenefit))+'</div>'+zoneRows(w.zones)+'</div>';}).join('')+'</article>';}
  function logger(){var sec=document.getElementById('workout');if(!sec)return;sec.querySelector('#desktopLogger')?.remove();if(!desktop())return;state.logger=state.logger||latestDate();var rows=gymOn(state.logger),s=stats(rows),polar=workoutsOn(state.logger),lastPolar=polar.slice(-1)[0],programName=typeof getProgramType==='function'&&typeof dayName==='function'?getProgramType(dayName(state.logger)):null,rpes=rows.map(function(r){return num(r.rpe);}).filter(function(v){return v!=null;}),avgRpe=rpes.length?round(rpes.reduce(function(a,v){return a+v;},0)/rpes.length):null,polarStrip=lastPolar?'<div class="desktopPolarStrip">'+metric('Ort. HR',round(lastPolar.avgHR),'bpm')+metric('Maks. HR',round(lastPolar.maxHR),'bpm')+metric('Aktif kalori',round(lastPolar.activeCal),'kcal')+metric('Cardio Load',round(first(lastPolar.cardioLoad,lastPolar.trainingLoad)))+'</div>':'';sec.insertAdjacentHTML('beforeend','<div id="desktopLogger" class="desktopWorkspace loggerWorkspace"><header class="desktopHead"><div><span class="desktopKicker">Session Overview</span><h1>'+esc(programName||'Workout Logger')+'</h1><p>'+esc(displayDate(state.logger))+(lastPolar&&lastPolar.startTime?' · '+esc(lastPolar.startTime):'')+'</p></div>'+nav('logger',displayDate(state.logger))+'</header><div class="desktopGrid"><article class="desktopCard span12 desktopSessionHero compact"><div class="desktopCardHead"><div><span class="desktopKicker">Read-only Training Review</span><h2>'+esc(programName||'Workout günü')+'</h2></div><span class="desktopBadge">Read-only</span></div><div class="desktopMetricStrip">'+metric('Egzersiz',s.exercises||null)+metric('Set',s.sets||null)+metric('Tekrar',s.reps||null)+metric('Hacim',s.volume?round(s.volume):null,'kg')+metric('Ort. RPE',avgRpe)+'</div>'+polarStrip+'</article><article class="desktopCard span12 desktopExerciseArea"><div class="desktopCardHead"><h2>Egzersizler</h2><span class="desktopBadge">'+s.exercises+' hareket</span></div>'+workoutTable(rows)+'</article></div></div>');}
  function dailyView(){
    var host=document.getElementById('dailyReport');if(!host||!desktop())return;state.daily=state.daily||latestDate();var date=state.daily,session=sessionOn(date),gym=session.gym,s=stats(gym),act=activityOn(date),sleep=sleepOn(date),night=nightlyOn(date),load=loadOn(date),polar=session.polar,lastPolar=session.primaryPolar||polar.slice(-1)[0],note=noteOn(date),cards=[],programName=typeof getProgramType==='function'&&typeof dayName==='function'?getProgramType(dayName(date)):null,exerciseNames=[...new Set(gym.map(function(x){return x.exercise;}).filter(Boolean))],best=gym.slice().sort(function(a,b){return (num(b.weight)||0)*(num(b.reps)||0)-(num(a.weight)||0)*(num(a.reps)||0);})[0];
    var status='<div class="desktopNav">'+badge(session.source)+badge(sleep?'Sleep var':'Sleep yok')+'</div>',snapshot='<article class="desktopCard span12 desktopDailySnapshot"><div class="snapshotPrimary"><div class="snapshotIcon">🏆</div><span class="desktopKicker">Daily Snapshot</span><h2>'+esc(displayDate(date))+'</h2><h3>'+esc(programName||label(lastPolar&&lastPolar.workoutType||session.appleLegacy&&session.appleLegacy.activityType||'Workout yok'))+'</h3>'+status+'<p>'+esc(exerciseNames.slice(0,5).join(' · ')||'Kayıtlı egzersiz yok')+'</p></div><div class="snapshotKpis">'+metric('Toplam workout',sessionCount(date)||null)+metric('Egzersiz',s.exercises||null)+metric('Set',s.sets||null)+metric('Tekrar',s.reps||null)+metric('Hacim',s.volume?round(s.volume):null,'kg')+metric('Süre',duration(lastPolar&&lastPolar.duration||session.appleLegacy&&session.appleLegacy.duration))+'</div><div class="snapshotHighlights"><div class="desktopListItem"><b>Best Performance</b>'+esc(best?best.exercise+' · '+round(best.weight)+' kg × '+round(best.reps):'Henüz veri yok')+'</div><div class="desktopListItem"><b>Extra Activity</b>'+esc(session.extraPolar.length?session.extraPolar.map(function(x){return label(x.workoutType||x.activityType);}).join(' · '):(act?label(act.activityType||act.workoutType||'Polar Activity'):'Yok'))+'</div><div class="desktopListItem"><b>Workout Source</b>'+esc(session.source||'Yok')+'</div></div></article>';cards.push(snapshot);
    if(act)cards.push(card('Polar Activity','<div class="desktopMetrics">'+metric('Adım',first(act.steps,act.stepCount))+metric('Aktif kalori',first(act.activeCalories,act.activeCal),'kcal')+metric('Aktivite süresi',duration(first(act.activeTime,act.duration)))+'</div>','span4'));
    if(sleep){var sleepScore=first(sleep.sleepScore),sleepMinutes=first(sleep.durationMinutes,sleep.durationSeconds!=null?sleep.durationSeconds/60:null);cards.push(card('Sleep','<div class="desktopVisualHero">'+ring(sleepScore,'Sleep','#8b5cf6')+'<div class="desktopHeroCopy"><b class="desktopHeroValue">'+esc(duration(sleepMinutes)||'—')+'</b></div></div>'+stageRows(sleep),'span4 tonePurple'));}
    if(night){var nightly=first(night.nightlyRechargeStatus,night.status);cards.push(card('Recovery','<div class="desktopVisualHero">'+ring(first(night.recoveryScore,night.nightlyRechargeScore),'Recovery','#80c72e',friendly(nightly,'nightly')||'Sinyal')+'<div class="desktopHeroCopy">'+badge(nightly,'nightly')+'</div></div><div class="desktopMetrics">'+metric('HRV',first(night.heartRateVariabilityAvg,night.hrv),'ms')+metric('Dinlenik nabız',first(night.heartRateAvg,night.restingHR),'bpm')+'</div>','span4 toneGreen'));}
    if(load){var cardio=first(load.cardioLoad,load.load),strain=first(load.strain),tolerance=first(load.tolerance);cards.push(card('Cardio Load','<div class="desktopVisualHero">'+ring(first(cardio,strain),'Load','#10b9ff')+'<div class="desktopHeroCopy">'+badge(friendly(load.status))+'</div></div><div class="desktopMetrics">'+metric('Strain',round(strain))+metric('Tolerance',round(tolerance))+'</div>','span4 toneOrange'));}
    if(polar.length)cards.push(polarSummary(date));if(gym.length)cards.push('<article class="desktopCard span12 desktopDisclosureCard"><details><summary><span><b>Gym Workout Details</b><small>'+s.exercises+' egzersiz · '+s.sets+' set</small></span><i>Detayları aç</i></summary>'+workoutTable(gym)+'</details></article>');if(note)cards.push(card('Günlük notlar','<div class="desktopListItem">'+esc(note.note||note.notes||note.text||JSON.stringify(note))+'</div>','span12'));if(!gym.length&&!polar.length&&!act&&!sleep&&!night&&!load)cards.push(card('Boş gün','<div class="desktopEmpty">Bu tarih için kayıtlı veri yok.</div>','span12'));
    host.innerHTML='<div class="desktopWorkspace"><header class="desktopHead"><div><span class="desktopKicker">Daily Performance Report</span><h1>'+esc(displayDate(date))+'</h1><p>Seçili günün sağlık, aktivite ve antrenman kayıtları.</p></div>'+nav('daily',displayDate(date))+'</header><div class="desktopGrid">'+cards.join('')+'<article class="desktopCard span12 desktopActionBar"><p><b>Daily JSON Report</b><span>Seçili günün temiz ve analiz edilebilir veri paketi.</span></p><button class="desktopExport" onclick="desktopExportDaily()">Export Daily JSON</button></article></div></div>';
  }
  function rangeDates(start,count){var out=[];for(var i=0;i<count;i++)out.push(add(start,i));return out;}
  function aggregate(dates){var gym=dates.flatMap(gymOn),polar=dates.flatMap(workoutsOn),activities=dates.map(activityOn).filter(Boolean),sleeps=dates.map(sleepOn).filter(Boolean),nights=dates.map(nightlyOn).filter(Boolean),loads=dates.map(loadOn).filter(Boolean),s=stats(gym);function avg(arr,fn){var values=arr.map(fn).map(num).filter(function(v){return v!=null;});return values.length?round(values.reduce(function(a,v){return a+v;},0)/values.length):null;}var gymDays=new Set(gym.map(function(x){return x.date;})),polarDays=new Set(polar.map(function(x){return x.date;})),activeDays=new Set(activities.map(function(x){return x.date;}));gymDays.forEach(activeDays.add,activeDays);polarDays.forEach(activeDays.add,activeDays);var exCounts={};gym.forEach(function(x){if(x.exercise)exCounts[x.exercise]=(exCounts[x.exercise]||0)+1;});return {gym:gym,polar:polar,activities:activities,sleeps:sleeps,nights:nights,loads:loads,stats:s,activeDays:activeDays.size,workoutDays:new Set([].concat(Array.from(gymDays),Array.from(polarDays))).size,restDays:Math.max(0,dates.length-new Set([].concat(Array.from(gymDays),Array.from(polarDays))).size),activeCalories:activities.reduce(function(a,x){return a+(first(x.activeCalories,x.activeCal)||0);},0)||null,avgSleep:avg(sleeps,function(x){return first(x.durationMinutes,x.durationSeconds!=null?x.durationSeconds/60:null);}),avgSleepScore:avg(sleeps,function(x){return x.sleepScore;}),avgHrv:avg(nights,function(x){return first(x.heartRateVariabilityAvg,x.hrv);}),avgRhr:avg(nights,function(x){return first(x.heartRateAvg,x.restingHR);}),avgRpe:avg(gym,function(x){return x.rpe;}),topExercises:Object.keys(exCounts).sort(function(a,b){return exCounts[b]-exCounts[a];}).slice(0,5).map(function(k){return {name:k,sets:exCounts[k]};})};}
  function weekly(){
    var host=document.getElementById('weeklyReport');if(!host||!desktop())return;state.week=state.week||monday(latestDate());var dates=rangeDates(state.week,7),end=dates[6],a=aggregate(dates),dailyStats=dates.map(function(d){var rows=gymOn(d),s=stats(rows);return {date:d,sets:s.sets,volume:s.volume};}),volumes=dailyStats.map(function(x){return x.volume;}),parts={};a.gym.forEach(function(r){if(!r.bodyPart)return;parts[r.bodyPart]=(parts[r.bodyPart]||0)+(num(r.weight)||0)*(num(r.reps)||0);});var palette=['#2f80ff','#18c8ff','#26d98b','#8b63ff','#ffac32','#ff4f68'],partDonut=donut(Object.keys(parts).map(function(k,i){return {label:k,value:parts[k],color:palette[i%palette.length]};})),bestDay=dailyStats.slice().sort(function(x,y){return y.volume-x.volume;})[0],setDay=dailyStats.slice().sort(function(x,y){return y.sets-x.sets;})[0],best=a.gym.slice().sort(function(x,y){return (num(y.weight)||0)*(num(y.reps)||0)-(num(x.weight)||0)*(num(x.reps)||0);})[0];
    var kpis='<div class="desktopKpiStrip weeklyKpis">'+metric('Toplam Hacim',a.stats.volume?round(a.stats.volume):null,'kg')+metric('Toplam Set',a.stats.sets||null)+metric('Toplam Tekrar',a.stats.reps||null)+metric('Ort. RPE',a.avgRpe)+'</div>',analysis='<article class="desktopCard span8 weeklyVolumeChart"><div class="desktopCardHead"><h2>Hacim Trendi</h2>'+badge(bestDay&&bestDay.volume?round(bestDay.volume)+' kg peak':null)+'</div>'+lineSpark(volumes,'#18c8ff')+'</article>'+(partDonut?'<article class="desktopCard span4"><h2>Kas Grubu Dağılımı</h2>'+partDonut+'</article>':'');
    var highlights='<div class="desktopHighlightGrid"><div><small>En İyi Gün</small><b>'+esc(bestDay&&bestDay.volume?displayDate(bestDay.date,{day:'numeric',month:'short'}):'—')+'</b><span>'+esc(bestDay&&bestDay.volume?round(bestDay.volume)+' kg hacim':'Veri yok')+'</span></div><div><small>En Çok Set</small><b>'+esc(setDay&&setDay.sets?displayDate(setDay.date,{day:'numeric',month:'short'}):'—')+'</b><span>'+esc(setDay&&setDay.sets?setDay.sets+' set':'Veri yok')+'</span></div><div><small>En Yüksek Performans</small><b>'+esc(best&&best.exercise||'—')+'</b><span>'+esc(best?round(best.weight)+' kg × '+round(best.reps):'Veri yok')+'</span></div><div><small>Uyku Ortalaması</small><b>'+esc(duration(a.avgSleep)||'—')+'</b><span>'+esc(a.sleeps.length?a.sleeps.length+' gece':'Veri yok')+'</span></div></div>';
    host.innerHTML='<div class="desktopWorkspace"><header class="desktopHead"><div><span class="desktopKicker">Weekly Performance Summary</span><h1>'+esc(displayDate(state.week,{day:'numeric',month:'short'}))+' — '+esc(displayDate(end,{day:'numeric',month:'short',year:'numeric'}))+'</h1><p>Haftalık performans, hacim ve kalite özeti.</p></div>'+nav('week',displayDate(state.week,{day:'numeric',month:'short'})+' — '+displayDate(end,{day:'numeric',month:'short'}))+'</header>'+kpis+'<div class="desktopGrid weeklyReferenceGrid">'+analysis+'<article class="desktopCard span12">'+highlights+'</article><article class="desktopCard span12 desktopActionBar"><p><b>Haftalık Not</b><span>Gerçek antrenman ve recovery kayıtlarının kompakt özeti.</span></p><div class="desktopNav"><button onclick="window.copyWeeklyCoachReport&&window.copyWeeklyCoachReport()">Copy Weekly Report</button><button class="desktopExport" onclick="desktopExportWeekly()">Export Weekly JSON</button></div></article></div></div>';
  }
  function monthly(){
    var host=document.getElementById('monthlyReport');if(!host||!desktop())return;state.month=state.month||monthKey(latestDate());var p=state.month.split('-').map(Number),last=new Date(p[0],p[1],0).getDate(),dates=rangeDates(state.month+'-01',last),a=aggregate(dates),painDays=new Set(a.gym.filter(function(x){return x.pain&&String(x.pain).toLowerCase()!=='none';}).map(function(x){return x.date;})),highRpeDays=new Set(a.gym.filter(function(x){return num(x.rpe)>=9;}).map(function(x){return x.date;})),goodFormDays=new Set(a.gym.filter(function(x){return /good|iyi/i.test(String(x.form||''));}).map(function(x){return x.date;})),weeks=[];
    for(var i=0;i<dates.length;i+=7){var wd=dates.slice(i,i+7),wa=aggregate(wd);weeks.push({label:(i/7+1)+'. hafta',workouts:wd.reduce(function(sum,date){return sum+sessionCount(date);},0),volume:round(wa.stats.volume)||0});}var sleepTrend=dates.map(function(d){var s=sleepOn(d);return first(s&&s.sleepScore,s&&s.durationMinutes);}),hasSleep=sleepTrend.some(function(x){return num(x)!=null;}),exerciseItems=a.topExercises.slice(0,7).map(function(x){return {label:x.name,value:x.sets,display:x.sets+' set'};}),quality=metric('Pain günü',painDays.size||null)+metric('Yüksek RPE',highRpeDays.size||null)+metric('İyi form günü',goodFormDays.size||null),kpis='<div class="desktopKpiStrip monthlyReferenceKpis">'+metric('Aktif Gün',a.activeDays)+metric('Workout Günü',a.workoutDays)+metric('Polar Workout',a.polar.length||null)+metric('Toplam Set',a.stats.sets||null)+metric('Toplam Tekrar',a.stats.reps||null)+metric('Toplam Hacim',a.stats.volume?round(a.stats.volume):null,'kg')+metric('Ort. Uyku',duration(a.avgSleep))+'</div>';
    var charts='<article class="desktopCard span3 monthlyVolumeTrend"><h2>Haftalık Hacim</h2>'+bars(weeks.map(function(w){return {label:w.label,value:w.volume,display:w.volume+' kg'};}))+'</article><article class="desktopCard span3 monthlyWorkoutTrend"><h2>Workout Sayısı</h2>'+bars(weeks.map(function(w){return {label:w.label,value:w.workouts,display:w.workouts};}))+'</article>'+(hasSleep?'<article class="desktopCard span3 tonePurple"><h2>Uyku / Sleep Score</h2>'+lineSpark(sleepTrend,'#8b63ff')+'<div class="desktopMetrics">'+metric('Ort. Sleep Score',a.avgSleepScore)+metric('Ort. Uyku',duration(a.avgSleep))+'</div></article>':'')+'<article class="desktopCard '+(hasSleep?'span3':'span6')+' monthlyExerciseFrequency"><h2>Egzersiz Sıklığı</h2>'+(exerciseItems.length?bars(exerciseItems):'<div class="desktopEmpty">Yeterli egzersiz verisi yok.</div>')+'</article>';
    host.innerHTML='<div class="desktopWorkspace"><header class="desktopHead"><div><span class="desktopKicker">Premium Monthly Review</span><h1>'+esc(displayDate(state.month+'-01',{month:'long',year:'numeric'}))+'</h1><p>Aylık performans ve sağlık görünümü.</p></div>'+nav('month',displayDate(state.month+'-01',{month:'long',year:'numeric'}))+'</header>'+kpis+'<div class="desktopGrid monthlyReferenceGrid">'+charts+(quality?'<article class="desktopCard span12 toneOrange monthlyQuality"><div class="desktopCardHead"><h2>Kalite Sinyalleri</h2></div><div class="desktopMetrics">'+quality+'</div></article>':'')+'<article class="desktopCard span12 desktopActionBar"><p><b>Monthly JSON Report</b><span>Aylık özet, haftalar ve günlük kayıtlar.</span></p><button class="desktopExport" onclick="desktopExportMonthly()">Export Monthly JSON</button></article></div></div>';
  }
  function programView(){
    var host=document.getElementById('programReport');if(!host||!desktop())return;var start=monday(latestDate()),dates=rangeDates(start,7),a=aggregate(dates),planned=dates.filter(function(d){return typeof getProgramType==='function'&&typeof dayName==='function'&&!/off/i.test(getProgramType(dayName(d)));}),done=new Set(a.gym.map(function(x){return x.date;})),adherence=planned.length?Math.round(planned.filter(function(d){return done.has(d);}).length/planned.length*100):null,byEx={};
    (data().workouts||[]).forEach(function(r){if(r.exercise)(byEx[r.exercise]=byEx[r.exercise]||[]).push(r);});var allProgress=Object.keys(byEx).map(function(ex){var rows=byEx[ex].slice().sort(function(x,y){return String(x.date).localeCompare(String(y.date));}),last=rows[rows.length-1];return {ex:ex,last:last,s:stats(rows),target:(data().autoNextTargets||{})[ex]};}).sort(function(x,y){return String(y.last.date).localeCompare(String(x.last.date));}),progress=state.allExercises?allProgress:allProgress.slice(0,10),polarGroups={};
    a.polar.forEach(function(w){var k=label(w.workoutType||w.activityType);(polarGroups[k]=polarGroups[k]||[]).push(w);});var responses=Object.keys(polarGroups).filter(function(k){return polarGroups[k].length>=2;}).map(function(k){var rows=polarGroups[k];function av(field){var v=rows.map(function(x){return num(x[field]);}).filter(function(x){return x!=null;});return v.length?round(v.reduce(function(s,x){return s+x;},0)/v.length):null;}return '<div class="desktopResponseCard"><div><span class="desktopKicker">'+rows.length+' seans</span><h3>'+esc(k)+'</h3></div><div class="desktopMetrics">'+metric('Ort. HR',av('avgHR'),'bpm')+metric('Maks. HR',av('maxHR'),'bpm')+metric('Ort. Load',av('trainingLoad'))+'</div></div>';}).join(''),alerts=[];
    Object.keys(byEx).forEach(function(ex){var recent=byEx[ex].slice().sort(function(x,y){return String(x.date).localeCompare(String(y.date));}).slice(-3);if(recent.length>=2&&recent.filter(function(x){return num(x.rpe)>=9;}).length>=2)alerts.push({tone:'warn',text:ex+': art arda yüksek RPE'});if(recent.filter(function(x){return x.pain&&String(x.pain).toLowerCase()!=='none';}).length>=2)alerts.push({tone:'danger',text:ex+': tekrarlayan Pain kaydı'});if(recent.filter(function(x){return x.form&&/bad|poor|kötü/i.test(x.form);}).length>=2)alerts.push({tone:'warn',text:ex+': tekrarlayan kötü form'});});var explicitCats={};a.gym.forEach(function(x){var c=x.category||x.classification||x.movementCategory;if(c)explicitCats[c]=(explicitCats[c]||0)+1;});
    var tabs=segmented('program',[{id:'overview',label:'Overview'},{id:'exercises',label:'Exercises'},{id:'polar',label:'Polar Response'},{id:'alerts',label:'Alerts'}],state.programTab),content='';
    if(state.programTab==='overview')content='<div class="desktopGrid">'+card('Program Overview','<div class="desktopMetrics">'+metric('Planlanan gün',planned.length||null)+metric('Tamamlanan',planned.filter(function(d){return done.has(d);}).length||null)+metric('Kaçırılan gün',planned.filter(function(d){return !done.has(d);}).length||null)+metric('Program Adherence',adherence,'%')+'</div>','span4')+card('Program ölçümleri','<div class="desktopMetrics">'+metric('Progression',allProgress.length>=3?'3+ seans sonrası değerlendirilecek':'Henüz hesaplanamadı')+metric('Recovery Compatibility',a.nights.length>=3?'Yeterli veri birikiyor':'Henüz hesaplanamadı')+metric('Load Balance',a.loads.length>=3?'Yeterli veri birikiyor':'Henüz hesaplanamadı')+metric('Movement Quality',a.gym.filter(function(x){return x.form;}).length>=3?'Yeterli veri birikiyor':'Henüz hesaplanamadı')+'</div>','span4')+card('Program Balance',Object.keys(explicitCats).length?bars(Object.keys(explicitCats).map(function(k){return {label:k,value:explicitCats[k],display:explicitCats[k]+' set'};})):'<div class="desktopEmpty">Gerçek kategori alanı bulunan yeterli veri yok.</div>','span4')+'</div>';
    else if(state.programTab==='exercises')content='<div class="desktopGrid">'+card('Exercise Progress',progress.length?'<div class="desktopTableWrap"><table class="desktopTable"><thead><tr><th>Egzersiz</th><th>Son kayıt</th><th>Kilo</th><th>Tekrar</th><th>Hacim</th><th>RPE</th><th>Form</th><th>Pain</th><th>Next Target</th></tr></thead><tbody>'+progress.map(function(x){return '<tr><td><b>'+esc(x.ex)+'</b></td><td>'+esc(x.last.date)+'</td><td>'+value(round(x.last.weight),'kg')+'</td><td>'+value(round(x.last.reps))+'</td><td><b>'+value(round(x.s.volume),'kg')+'</b></td><td>'+stateBadge(x.last.rpe)+'</td><td>'+stateBadge(x.last.form)+'</td><td>'+stateBadge(x.last.pain)+'</td><td><div class="desktopTarget">'+value(x.last.nextSessionTarget||x.target&&x.target.text)+'</div></td></tr>';}).join('')+'</tbody></table></div>':'<div class="desktopEmpty">Egzersiz geçmişi yok.</div>','span12')+(allProgress.length>10?'<article class="desktopCard span12 desktopActionBar"><p><b>'+allProgress.length+' hareket bulundu</b><span>İlk hareketler son kayıt tarihine göre sıralanır.</span></p><button class="desktopExport" onclick="desktopToggleAllExercises()">'+(state.allExercises?'İlk 10’u göster':'Tümünü göster')+'</button></article>':'')+'</div>';
    else if(state.programTab==='polar')content='<div class="desktopGrid">'+card('Polar Response',responses||'<div class="desktopEmpty">Workout türü karşılaştırması için tür başına en az 2 Polar seans gerekli.</div>','span12')+'</div>';
    else content='<div class="desktopGrid">'+card('Program Alerts',alerts.length?'<div class="desktopAlertGrid">'+alerts.map(function(x){return '<div class="desktopAlertCard '+x.tone+'"><span>'+(x.tone==='danger'?'Risk':'Dikkat')+'</span><p>'+esc(x.text)+'</p></div>';}).join('')+'</div>':'<div class="desktopPositive"><b>✓ Belirgin risk sinyali yok</b><p>Program verilerinde belirgin bir risk sinyali bulunmadı.</p></div>','span12')+'</div>';
    host.innerHTML='<div class="desktopWorkspace"><header class="desktopHead"><div><span class="desktopKicker">Program Intelligence</span><h1>Program performansı</h1><p>Program uyumu, egzersiz ilerlemesi ve Polar yanıtı.</p></div><span class="desktopBadge">'+esc(displayDate(start,{day:'numeric',month:'short'}))+' haftası</span></header>'+tabs+'<div class="desktopTabPane">'+content+'</div></div>';
  }
  function polishProgram(){var host=document.getElementById('programReport');if(!host||!desktop())return;var cards=[].slice.call(host.querySelectorAll('.desktopCard')),overview=cards.find(function(x){return x.querySelector('h2')&&x.querySelector('h2').textContent==='Program Overview';});if(overview&&!overview.querySelector('.desktopProgramRing')){var start=monday(latestDate()),dates=rangeDates(start,7),planned=dates.filter(function(d){return typeof getProgramType==='function'&&typeof dayName==='function'&&!/off/i.test(getProgramType(dayName(d)));}),done=new Set(aggregate(dates).gym.map(function(x){return x.date;})),score=planned.length?Math.round(planned.filter(function(d){return done.has(d);}).length/planned.length*100):null;overview.classList.add('toneGreen');overview.insertAdjacentHTML('afterbegin','<div class="desktopVisualHero desktopProgramRing">'+ring(score,'Adherence','#80c72e')+'<div class="desktopHeroCopy"><b class="desktopHeroValue">'+esc(score==null?'Hesaplanamadı':score+'%')+'</b>'+progress(score,'green')+'<p>Planlanan ve tamamlanan günlerin gerçek DATA karşılaştırması.</p></div></div>');}cards.forEach(function(c){var title=c.querySelector('h2')?.textContent||'';if(title==='Program ölçümleri')c.classList.add('tonePurple');if(title==='Program Alerts'&&c.querySelector('.desktopListItem'))c.querySelectorAll('.desktopListItem').forEach(function(x,i){x.classList.add(i===0?'danger':'');});});}
  function polishWeekly(){var host=document.getElementById('weeklyReport');if(!host||!desktop()||host.querySelector('.weeklyHighlights'))return;var start=state.week||monday(latestDate()),dates=rangeDates(start,7),counts=dates.map(function(d){return {date:d,count:gymOn(d).length+workoutsOn(d).length};}).sort(function(a,b){return b.count-a.count;}),head=host.querySelector('.desktopHead>div');if(head&&counts[0]&&counts[0].count)head.insertAdjacentHTML('beforeend','<div class="weeklyHighlights">'+badge('En yoğun gün · '+displayDate(counts[0].date,{day:'numeric',month:'short'}))+'</div>');}
  function polishMonthly(){var host=document.getElementById('monthlyReport');if(!host||!desktop())return;var first=host.querySelector('.monthlyExerciseFrequency .desktopBarRow');if(first)first.classList.add('top');var volume=host.querySelector('.monthlyVolumeTrend');if(volume)volume.classList.add('tonePurple');}
  function dataCenter(){var sec=document.getElementById('data');if(!sec)return;sec.querySelector('.desktopDataPolar')?.remove();if(!desktop())return;var d=data(),c=d.polarConnection||{},counts=c.counts||{};sec.querySelector('.topbar')?.insertAdjacentHTML('afterend','<div class="desktopDataPolar card"><div class="desktopCardHead"><div><h2>Polar Sync</h2><p class="desktopMuted">AccessLink bağlantısı ve tarayıcı DATA durumu.</p></div>'+badge(c.connected?'Bağlı':'Bağlı değil')+'</div><div class="desktopMetrics">'+metric('Son başarılı sync',c.lastSyncAt)+metric('Workout',counts.workouts!=null?counts.workouts:Object.keys((d.polarWorkouts||{}).daily||{}).length)+metric('Activity',counts.activity!=null?counts.activity:Object.keys((d.polarActivity||{}).daily||{}).length)+metric('Sleep',counts.sleep!=null?counts.sleep:Object.keys((d.polarSleep||{}).daily||{}).length)+'</div><div class="actions"><button class="btn" onclick="simurgPolarSyncNow&&simurgPolarSyncNow()">Manuel Polar Sync</button></div></div>');}
  function renderAll(){ensureNav();if(!desktop()){document.querySelectorAll('.desktopWorkspace,.desktopDataPolar').forEach(function(x){x.remove();});return;}home();logger();dailyView();weekly();polishWeekly();monthly();polishMonthly();programView();polishProgram();dataCenter();}
  window.desktopOpen=function(id){var b=[].slice.call(document.querySelectorAll('aside .nav button')).find(function(x){return (x.getAttribute('onclick')||'').indexOf("'"+id+"'")>-1;});if(typeof show==='function')show(id,b);};
  window.desktopSetHomeTab=function(tab){if(['overview','recovery','sleep','load'].indexOf(tab)<0)return;state.homeTab=tab;home();};
  window.desktopHomeMove=function(delta){state.homeDate=add(state.homeDate||dateNow(),delta);home();};
  window.desktopHomeToday=function(){state.homeDate=dateNow();home();};
  window.desktopHomeLatest=function(){state.homeDate=latestHomeDate();home();};
  window.desktopSetProgramTab=function(tab){if(['overview','exercises','polar','alerts'].indexOf(tab)<0)return;state.programTab=tab;programView();polishProgram();};
  window.desktopToggleAllExercises=function(){state.allExercises=!state.allExercises;programView();polishProgram();};
  window.desktopMove=function(kind,delta){if(kind==='daily'||kind==='logger')state[kind]=add(state[kind]||latestDate(),delta);else if(kind==='week')state.week=add(state.week||monday(latestDate()),delta*7);else if(kind==='month')state.month=monthShift(state.month||monthKey(latestDate()),delta);renderAll();};
  window.desktopLatest=function(kind){if(kind==='daily'||kind==='logger')state[kind]=latestDate();else if(kind==='week')state.week=monday(latestDate());else state.month=monthKey(latestDate());renderAll();};
  window.desktopExportDaily=function(){var date=state.daily||latestDate();downloadJson('simurg-daily-'+date+'.json',{exportType:'simurg_daily_summary',exportVersion:'1.0',date:date,generatedAt:new Date().toISOString(),analysisContext:{language:'tr',purpose:'daily_health_and_training_review'},data:dayPayload(date)});};
  window.desktopExportWeekly=function(){var start=state.week||monday(latestDate()),end=add(start,6),dates=rangeDates(start,7),a=aggregate(dates);downloadJson('simurg-weekly-'+start+'_'+end+'.json',{exportType:'simurg_weekly_summary',exportVersion:'1.0',weekStart:start,weekEnd:end,generatedAt:new Date().toISOString(),analysisContext:{language:'tr',purpose:'weekly_health_training_and_recovery_review'},summary:clean({activeDays:a.activeDays,workoutDays:a.workoutDays,restDays:a.restDays,gym:stats(a.gym),polarWorkouts:a.polar.length,sleepNights:a.sleeps.length,recoveryNights:a.nights.length,averageSleepMinutes:a.avgSleep,averageSleepScore:a.avgSleepScore,averageHrv:a.avgHrv,averageRestingHr:a.avgRhr}),days:dates.map(dayPayload).filter(function(x){return Object.keys(x).length>1;})});};
  window.desktopExportMonthly=function(){var ym=state.month||monthKey(latestDate()),p=ym.split('-').map(Number),last=new Date(p[0],p[1],0).getDate(),dates=rangeDates(ym+'-01',last),a=aggregate(dates),weeks=[];for(var i=0;i<dates.length;i+=7){var wd=dates.slice(i,i+7),wa=aggregate(wd);weeks.push(clean({start:wd[0],end:wd[wd.length-1],activeDays:wa.activeDays,workoutDays:wa.workoutDays,gym:stats(wa.gym),polarWorkouts:wa.polar.length}));}downloadJson('simurg-monthly-'+ym+'.json',{exportType:'simurg_monthly_review',exportVersion:'1.0',month:ym,generatedAt:new Date().toISOString(),analysisContext:{language:'tr',purpose:'monthly_health_training_recovery_and_program_review'},summary:clean({activeDays:a.activeDays,workoutDays:a.workoutDays,gym:stats(a.gym),polarWorkouts:a.polar.length,averageSleepMinutes:a.avgSleep,averageSleepScore:a.avgSleepScore,averageHrv:a.avgHrv,averageRestingHr:a.avgRhr}),weeks:weeks,days:dates.map(dayPayload).filter(function(x){return Object.keys(x).length>1;})});};
  var previousRender=window.render;if(typeof previousRender==='function'){window.render=function(){var result=previousRender.apply(this,arguments);setTimeout(renderAll,0);return result;};}
  addEventListener('resize',function(){clearTimeout(window.__desktopAlignmentResize);window.__desktopAlignmentResize=setTimeout(renderAll,120);});
  function initialRender(){setTimeout(renderAll,50);setTimeout(renderAll,450);setTimeout(renderAll,1250);}
  function guardReports(){['dailyReport','weeklyReport','monthlyReport','programReport'].forEach(function(id){var host=document.getElementById(id);if(!host)return;new MutationObserver(function(){if(!desktop()||host.querySelector('.desktopWorkspace'))return;setTimeout(function(){if(id==='dailyReport')dailyView();else if(id==='weeklyReport'){weekly();polishWeekly();}else if(id==='monthlyReport'){monthly();polishMonthly();}else{programView();polishProgram();}},0);}).observe(host,{childList:true});});}
  guardReports();
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',initialRender):initialRender();
})();
